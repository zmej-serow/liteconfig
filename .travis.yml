language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
after_success:
  - coveralls
script:
  - python -m unittest tests/test-*.py
  - coverage run --source=liteconfig/liteconfig.py tests/test-*.py
  - coverage report -m
